'use strict';
var PromisePage = function () {
    const array = [
        ['Yes', 'css path']

    ];
    var elementMap = new Map(array);

    //this.clickUpdateHref = function (atpUpdate, index) {
    //    element.all(by.repeater('')).filter(function (atpRow) {
    //        return atpRow.element(by.binding(('')).getText().then(function (text) {
    //            return text === atpUpdate;
    //        }))
    //    }).then(function (atpRows) {
    //        atpRows[index].element(by.css('')).click();
    //    }); //<tr ng-repeat>
    //}
};
module.exports = new PromisePage();
